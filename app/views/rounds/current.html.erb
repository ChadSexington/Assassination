<h1>Round <%= @round.id %></h1>
<div class="well">
  <div class="row">
    <div class="col-md-10">
      <dl class="dl-horizontal">
        <dt>Activation date:</dt>
          <dd><%= parse_date @round.created_at %></dd>
        <dt>Start date:</dt>
          <dd><%= parse_date @round.start_time %></dd>
        <dt>End date:</dt>
          <dd><%= parse_date @round.end_time %></dd>
        <dt>Kill -9 start:</dt>
          <% if @round.kill9 %>
          <dd><%= parse_date @round.kill9_start_time %></dd>
          <% else %>
          <dd>-</dd>
          <% end %>
        <dt>Number of players:</dt>
          <dd><%= @round.players.count %></dd>
        <dt>Active Assignments:</dt>
          <dd><%= @round.assignments.where(:active => true).count %></dd>
      </dl>
    </div>
    <div class="col-md-2">
      <div class="well well-small">
      <h4>Started:</h4>
      <% if @round.started? %>
        <span class="glyphicon glyphicon-ok"></span>
      <% else %>
        <span class="glyphicon glyphicon-remove"></span>
      <% end %>
      </div>
    </div>
  </div>
</div>
