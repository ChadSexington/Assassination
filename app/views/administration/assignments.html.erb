<h1>Administer assignments</h1>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Owner</th>
      <th>Target</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <% @assignments.each do |ass| %>
      <tr>
        <td><%= link_to Player.find(ass.player_id).name, edit_assignment_path(ass) %></td>
        <td><%= Player.find(ass.target).name %></td>
        <td><%= if ass.status == 0; "Ongoing"; else; "Completed"; end%></td>
      </tr>
    <% end %>
  </tbody>
</table>
